services:
  app:
    container_name: app
    build: ./
    ports:
      - '3000:3000'
    volumes:
      - '.:/app'
      - /app/node_modules
    depends_on:
      - mongo

  mongo:
    restart: always
    image: mongo
    volumes:
      - ./data:/data/db
    expose:
      - 27017